DO FIRST Update the shapemaking in test-harmonise_crs.R to match the OS grid
examples used in test-crop_to_aoi.R

Is the example in load_shape() OK? It works but looks messy as it generates the
data.

Swap in better testing data, particularly for the inst/extdata stuff.
I think it makes sense manually create small data objects in the example and
test scripts most of the time actually (for the bit functions at least).

Check in which individual functions get @export in roxygen.

